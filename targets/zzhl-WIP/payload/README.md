# ZZHL native payload source

`src/` is a target-local copy of the native P0 payload source and ZZHL target headers. The root `Makefile` selects this directory for `TARGET=dm3q-S918BXXUAZZHL`. The ZZHL open engine P0 route also imports these sources.
